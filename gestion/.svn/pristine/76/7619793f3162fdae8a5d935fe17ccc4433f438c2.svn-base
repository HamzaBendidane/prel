<?php
    
    /* Médiathèque Bloc Les Essentiels */
    if (count($essentials) > 0) :
        print '<div class="essentials-medias">';

            print '<div class="rubrique-essentials">Les Essentiels</div>';

            print '<div class="essentials-list">';

                foreach ($essentials as $essential) {
                    // Appeler le template pour afficher la pagination des résultats :
                    print theme(
                        'mediatheque_media_row_theme',
                        array(
                            'element' => $essential,
                        )
                    );
                }

            print '</div>';

        print '</div>';
        
    endif;