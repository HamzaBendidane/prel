<?php
    
    print '<div class="filters-order">';
        if ($nbResultTot > 1):
            print '<div class="nb-results">'.$nbResultTot.' médias disponibles</div>';
        else:
            print '<div class="nb-results">'.$nbResultTot.' média disponible</div>';
        endif;
        print render($formOrder);
    print '</div>';

    print '<br /><br />';

    /* Results list */
    print '<div class="results-medias">';
        foreach ($essentials as $essential) {
            // Appeler le template pour afficher la pagination des résultats :
            print theme(
                'mediatheque_media_row_theme',
                array(
                    'element' => $essential,
                )
            );
        }

        foreach ($results as $result) {
            // Appeler le template pour afficher la pagination des résultats :
            print theme(
                'mediatheque_media_row_theme',
                array(
                    'element' => $result,
                )
            );
        }
    print '</div>';
    
    print '<br /><br />';
    
    
    /* Results Pager */
    print theme(
        'mediatheque_pager_theme',
        array(
            'nbResultTot' => $nbResultTot,
            'pageCurrent' => $pageCurrent,
            'nbElementDisplay' => $nbElementDisplay,
        )
    ); 